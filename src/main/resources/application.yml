spring:
  datasource:
    url: jdbc:mysql://localhost:3306/awesome-jpa
    username: root
    password: verysecretpass
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    show-sql: true
    properties:
      hibernate:
        format_sql: true
